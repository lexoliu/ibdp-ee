\documentclass{article}
\usepackage[margin=1in]{geometry}
\usepackage{titlesec}
\usepackage[hidelinks]{hyperref}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{natbib}
\usepackage{tikz}
\usepackage{graphicx}
\usepackage{svg}
\usepackage{capt-of}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric}

% --- Code Listing Styles ---
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=2
}
\lstset{style=mystyle}

% Style for shell scripts
\lstdefinestyle{shstyle}{
    language=bash,
    basicstyle=\ttfamily\footnotesize,
    keywordstyle=\color{blue},
    commentstyle=\color{codegreen},
    stringstyle=\color{codepurple},
    backgroundcolor=\color{backcolour},
    showstringspaces=false,
    numbers=left,
    numberstyle=\tiny\color{codegray},
    numbersep=5pt,
    breaklines=true,
    captionpos=b,
}

% Style for Python
\lstdefinestyle{pythonstyle}{
    language=Python,
    basicstyle=\ttfamily\footnotesize,
    keywordstyle=\color{blue},
    commentstyle=\color{codegreen},
    stringstyle=\color{codepurple},
    backgroundcolor=\color{backcolour},
    morekeywords={*,plt,pd,sys,os,re},
    showstringspaces=false,
    numbers=left,
    numberstyle=\tiny\color{codegray},
    numbersep=5pt,
    breaklines=true,
    captionpos=b,
}

\title{Performance Impact of Garbage Collection in Real-World Applications}
\author{Lexo Liu}

\begin{document}

\begin{titlepage}
\centering
{\huge\bfseries Performance Impact of Garbage Collection in Real-World Applications\par}
\vspace{2cm}
{\large\bfseries How do performance characteristics vary between GC and non-GC languages?\par}
\vspace{1.5cm}
{\large Subject: Computer Science\par}
\end{titlepage}

\tableofcontents
\newpage

\section{Introduction}

Modern programming languages face a fundamental trade-off in memory management: explicit control versus automated safety. Languages like C and Rust demand manual memory management, offering precise control but requiring careful programming to avoid crashes and security vulnerabilities. In contrast, garbage-collected languages like Java and Go automate memory reclamation, reducing bugs but potentially introducing runtime overhead that affects application performance.

While garbage collection theory is well-established, empirical studies comparing GC and non-GC languages often conflate multiple performance factors. This research specifically isolates the impact of garbage collection by implementing algorithmically identical services across three strategically chosen languages: Rust (AOT-compiled, manual memory management), Go (AOT-compiled, garbage collected), and Java (JIT-compiled, garbage collected). This selection enables controlled comparison across both memory management strategies (manual vs. GC) and compilation approaches (AOT vs. JIT) to answer: \textbf{How do performance characteristics vary between GC and non-GC languages?}

The study evaluates three workload categories—compute-intensive, serialization, and allocation-heavy key-value operations—measuring throughput, latency, and memory consumption under controlled conditions. This approach provides evidence-based insights for developers making language selection decisions in performance-critical applications.

\section{Background: Memory Management in Programming Languages}

\subsection{Memory Management Fundamentals}

Memory management shapes how programs allocate, reuse, and eventually release memory. Most languages separate stack and heap usage. Stack frames hold local variables and disappear automatically when a function returns, so programmers rarely intervene. Heap allocations last longer and require either explicit control (as in C or Rust) or a runtime that reconciles unused objects on the developer's behalf.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=1.2, every node/.style={font=\small}]
% Memory layout
\draw[thick] (0,0) rectangle (4,6);
\node[anchor=west] at (4.2,5.8) {Higher Address};
\node[anchor=west] at (4.2,0.2) {Lower Address};
% Stack region
\draw[dashed] (0,4.5) -- (4,4.5);
\node at (2,5.3) {\textbf{Stack (LIFO)}};
\draw[fill=blue!10] (0.5,4.6) rectangle (3.5,5.2);
\node at (2,4.9) {Function Call Frame};
\draw[fill=blue!10] (0.5,3.8) rectangle (3.5,4.4);
\node at (2,4.1) {Local Variables};
\draw[->, thick] (2,3.6) -- (2,2.7);
\node at (3,3.1) {Grows Downward};

% Heap region
\node at (2,1.3) {\textbf{Heap (Dynamic)}};
\draw[fill=red!10] (0.5,1.2) rectangle (3.5,1.9);
\node at (2,1.55) {Object A};
\draw[fill=red!10] (0.5,0.4) rectangle (3.5,1.1);
\node at (2,0.75) {Object B};
\draw[->, thick] (2,2.0) -- (2,3.0);
\node at (3,2.5) {Grows Upward};

% Label
\node[anchor=west] at (4.2,4.5) {Stack/Heap Boundary};
\end{tikzpicture}
\caption{Stack and Heap Memory Layout in a Typical Program }
\end{figure}

Understanding the differences between stack and heap memory is crucial for analyzing how programming languages manage memory. In the following sections, we will explore how heap memory is managed through manual techniques and automatic algorithms such as garbage collection.

\subsection{The Challenge of Manual Memory Management}

Early high-level languages such as C and C++ placed all responsibility for heap lifetime on the programmer. The approach delivers raw speed, yet common mistakes—memory leaks, dangling pointers, or double frees—can crash applications or open security holes. These recurring issues motivated automated schemes that keep the performance benefits while avoiding the sharpest edges.

\subsubsection*{Manual Memory Management Examples in C}

\begin{lstlisting}[language=C, caption=Memory Leak Example, style=mystyle]
#include <stdlib.h>

void memory_leak() {
    int* data = (int*) malloc(100 * sizeof(int));
    // Function ends without freeing 'data'
    // This memory remains allocated but inaccessible
}
\end{lstlisting}

\subsection{The Evolution to Automatic Memory Management}

As software systems grew in complexity, the burden of manual memory management became increasingly problematic. This led to the development of automatic memory management techniques that could reliably handle memory allocation and deallocation without programmer intervention. Two primary approaches emerged: reference counting and tracing garbage collection.

\subsubsection{Reference Counting: A First Step}

Reference counting keeps a per-object counter that increments with new references and decrements as they disappear. It spreads reclamation cost across program execution, yet cycles remain a weak point: two objects that point to each other never reach zero and therefore leak.

\subsection{Tracing Garbage Collection: A Comprehensive Solution}

Tracing collectors treat the heap as a graph rooted in stack and global references. They mark every reachable object and reclaim whatever remains, naturally handling cycles and most real-world allocation patterns.

\subsubsection{Mark-and-Sweep: The Archetypal Tracing Algorithm}

Mark-and-sweep alternates two passes. A mark phase follows pointers from the roots to label live objects; a sweep phase scans memory and frees anything unmarked. Modern collectors build on this template with generational heaps or concurrent marking, but the core intuition is unchanged.

\subsection{The Performance Overhead of Garbage Collectors}
"No silver bullet" applies here as well: removing manual memory bugs means accepting some runtime cost. Garbage collectors can introduce brief pauses, reserve extra heap space, and consume CPU cycles to locate dead objects. Quantifying when those effects matter is the empirical focus of the remainder of this essay.

\section{Literature Review}


\subsection{Historical and Contemporary Studies on Garbage Collection}

The trade-off between manual memory management and garbage collection has been extensively studied in both systems and programming language research. Early studies such as \citet{wilson1992uniprocessor} emphasized the runtime overhead of tracing collectors, showing that stop-the-world pause times could grow linearly with heap size. Later work focused on improving collector design, for example through generational collection \citep{unger1984generation}, concurrent mark-and-sweep \citep{dijkstra1978concurrent}, and modern pauseless algorithms such as G1 and ZGC in Java \citep{shipilev2019zgc}. These advances demonstrate that garbage collectors are not static technologies but continuously evolving to minimize latency and maximize throughput.

Empirical performance comparisons across programming languages have been carried out in a number of benchmarking studies. The Computer Language Benchmarks Game\footnote{\url{https://benchmarksgame-team.pages.debian.net/benchmarksgame/}} provides a wide-ranging dataset comparing implementations of standard algorithms across C, Java, Go, and others. However, critics note that such benchmarks often conflate language design with implementation quality and do not isolate the specific contribution of garbage collection overhead \citep{sewell2012performance}. Other applied studies have investigated GC in the context of managed runtimes for web servers and databases \citep{domani2000gcserver}, showing measurable pause-induced latency outliers in high-throughput systems.

\subsection{Gap in Existing Research}

Despite decades of progress in garbage collection theory and engineering, there remains a shortage of experimental work that focuses on isolating GC overhead in realistic, end-to-end application workloads. Prior studies often rely on microbenchmarks or compiler-level analysis, which are valuable but do not directly inform practitioners developing web services or distributed systems today. In addition, much of the literature predates modern concurrent and low-latency collectors, making their conclusions less representative of current environments.

Therefore, this project seeks to contribute by:
\begin{itemize}
    \item Narrowing scope to compare GC vs. manual memory management in languages with broadly similar compilation strategies (Rust, Go, Java).
    \item Using application-level workloads (echo server, key-value store, JSON/serialization tasks, prime computation) rather than synthetic allocation loops.
    \item Reporting not only throughput and latency, but also memory consumption trends, which are underexplored in existing studies.
\end{itemize}

\section{Methodology}
\section{Methodology}
\label{sec:methodology}

\subsection{Experimental Setup}

The experimental design compared three languages representing different compilation and memory management strategies:

\begin{itemize}
    \item \textbf{Rust}: AOT-compiled with manual memory management, providing a baseline for deterministic performance
    \item \textbf{Go}: AOT-compiled with concurrent garbage collection, isolating GC overhead from JIT effects
    \item \textbf{Java}: JIT-compiled with generational garbage collection, representing managed runtime environments
\end{itemize}

This selection enables controlled analysis by separating garbage collection effects from compilation strategy effects, while covering the most common approaches in modern systems programming.

Benchmarks were executed in two complementary environments to capture both on-device and hosted deployments:

\begin{itemize}
    \item \textbf{Local workstation (baseline)}: MacBook Pro with an Apple M2 Max (12 performance + 4 efficiency cores) and 32~GB unified memory running macOS 15.6. The Go 1.22 toolchain, Rust 1.80 (cargo), and OpenJDK~21/Spring Boot~3.3.3 were installed natively. Both the service under test and the \texttt{k6} client executed on this host for baseline measurements.
    \item \textbf{Aliyun hosted environment}: Tokyo region deployment managed by the benchmark harness. The load generators ran on a dedicated 2~vCPU / 4~GB ECS instance (client), while services were launched on an 8~vCPU / 32~GB ECS instance (server). The same language toolchains were provisioned via the project scripts to ensure parity with the local setup.
\end{itemize}

For each run the harness ensured exclusive access by stopping any previously running service, freeing bound ports, and resetting the manager state before the next language started. Local runs bound to \texttt{127.0.0.1:8080}; cloud runs used the manager-reported public endpoint with identical health checks and orchestration logic.

\subsection{Workload Design}

Three workload groups were selected to represent real-world application patterns:

\begin{table}[h]
  \centering
  \begin{tabular}{@{}lll@{}}
  \toprule
  Group & Endpoint(s) & Characteristics \\
  \midrule
  Prime & \texttt{/is\_prime} & CPU-bound, minimal allocation \\
  Light & \texttt{/echo}, \texttt{/json}, \texttt{/json2xml} & Mixed serialization, moderate allocation \\
  KV & \texttt{/kv/get}, \texttt{/kv/set}, \texttt{/kv/delete} & I/O-bound, allocation-heavy, persistent state \\
  \bottomrule
  \end{tabular}
  \caption{Workload categories and their characteristics.}
  \label{tab:workloads}
\end{table}

This taxonomy allows analysis across different stress patterns: purely computational, moderately memory-intensive, and allocation-heavy workloads.

\subsection{Load Generation and Metrics}

Workloads were stressed using the \texttt{k6} load testing framework in \texttt{normal} mode:

\begin{itemize}
    \item Virtual Users (VUs): 64 across all experiments
    \item Duration: 10 minutes per attempt, matching the harness default for \texttt{normal} mode
    \item Repeats: two sequential attempts per workload, with the first run serving as a pre-warm phase and the second run providing the measurement data
    \item Base URL: \texttt{http://127.0.0.1:8080} for local runs and the manager-advertised service host for Aliyun runs
\end{itemize}

The pre-warm procedure was essential for ensuring fair comparison, particularly for Java's JIT compiler which requires initial execution to optimize hot code paths. This two-phase approach eliminated JIT warm-up effects from the measurements while capturing steady-state performance characteristics.

Each test produced JSON-formatted time-series data capturing:
\begin{itemize}
    \item \textbf{Throughput (requests/sec)} from \texttt{http\_reqs}
    \item \textbf{Latency (ms)} from \texttt{http\_req\_duration}
    \item \textbf{Failure Rate} from \texttt{http\_req\_failed}
\end{itemize}

In addition, memory consumption was sampled once per second using the \texttt{ps} command to record RSS (resident set size). These data were stored in CSV for later analysis.

\subsection{Data Analysis}

For each workload-language combination:
\begin{enumerate}
    \item Time-series data were aligned to a common start time.
    \item Mean throughput, median latency, and peak memory were computed.
    \item Comparative plots were generated for each workload group: throughput, latency, and memory trends.
\end{enumerate}

This analysis design enables both cross-language comparison (e.g., Rust vs. Go under \texttt{/kv/set}) and intra-language workload scaling (e.g., Java across prime vs. KV).

\section{Results \& Analysis}

This section presents the empirical findings of the benchmark experiments across three workload groups: \textbf{Prime (compute-intensive)}, \textbf{Light (serialization)}, and \textbf{KV (allocation-heavy key-value store)}. For each workload, we compare throughput (requests per second), average latency (ms), and memory consumption (RSS, MB) across Java, Go, and Rust implementations.

\subsection{Prime Workload: Compute-Intensive, Minimal Allocation}

\begin{figure}[h]
    \centering
    %\includegraphics[width=0.9\linewidth]{results/prime_throughput.png}%
    \caption{Throughput comparison in Prime workload.}
    \label{fig:prime-throughput}
\end{figure}

\begin{figure}[h]
    \centering
    %\includegraphics[width=0.9\linewidth]{results/prime_latency.png}%
    \caption{Latency comparison in Prime workload.}
    \label{fig:prime-latency}
\end{figure}

In the prime number benchmark, Rust and Go demonstrated nearly identical throughput and latency, with only minor fluctuations attributable to runtime scheduling. In contrast, Java’s throughput was significantly lower, lagging by an order of magnitude compared to both Rust and Go (Figure~\ref{fig:prime-throughput}). Average latencies for Java were consistently higher, even in the absence of dynamic allocations (Figure~\ref{fig:prime-latency}).

This result indicates that garbage collection is not the primary bottleneck in Java for this workload; instead, the performance gap arises from the runtime characteristics of the JVM, including warm-up time, JIT compilation overhead, and less efficient numeric operations compared to AOT-compiled Rust and Go binaries.

\subsection{Light Workload: Serialization and Moderate Allocation}

\begin{figure}[h]
    \centering
    \fbox{\rule{0pt}{2in}\rule{0.9\linewidth}{0pt}}
    \caption{Throughput comparison in Light workload.}
    \label{fig:light-throughput}
\end{figure}

\begin{figure}[h]
    \centering
    \fbox{\rule{0pt}{2in}\rule{0.9\linewidth}{0pt}}
    \caption{Latency distribution in Light workload.}
    \label{fig:light-latency}
\end{figure}

In the Light workload, which mixes echo, JSON serialization, and JSON-to-XML conversion, Rust again outperformed both GC-based languages in terms of raw throughput. Go’s throughput was slightly lower than Rust but remained close, with less than a 10\% difference. Java, while improved relative to its prime workload results, still fell behind both Go and Rust (Figure~\ref{fig:light-throughput}).

Latency distributions further highlighted the role of garbage collection (Figure~\ref{fig:light-latency}): Go experienced occasional spikes associated with minor GC cycles, while Rust showed consistently flat latency curves. Java, in addition to baseline runtime overhead, exhibited both higher median latency and more pronounced tail outliers, confirming that GC pauses and JVM scheduling contribute to unpredictability under moderate allocation pressure.

\subsection{KV Workload: Allocation-Heavy, Persistent State}

\begin{figure}[h]
    \centering
    \fbox{\rule{0pt}{2in}\rule{0.9\linewidth}{0pt}}
    \caption{Throughput comparison in KV workload.}
    \label{fig:kv-throughput}
\end{figure}

\begin{figure}[h]
    \centering
    \fbox{\rule{0pt}{2in}\rule{0.9\linewidth}{0pt}}
    \caption{Latency comparison in KV workload.}
    \label{fig:kv-latency}
\end{figure}

\begin{figure}[h]
    \centering
    \fbox{\rule{0pt}{2in}\rule{0.9\linewidth}{0pt}}
    \caption{Memory consumption in KV workload.}
    \label{fig:kv-memory}
\end{figure}

The KV workload, designed to mimic a simplified Redis-like server, was the most memory-intensive scenario. Here, differences in memory management strategies became most visible:

\begin{itemize}
    \item Rust exhibited the lowest and most stable RSS memory footprint, consistent with explicit, deterministic memory release.
    \item Go’s memory usage was higher but stable, demonstrating the effect of heap reservation and concurrent GC. Importantly, throughput remained comparable to Rust, suggesting Go’s GC is efficient enough to sustain high load.
    \item Java showed both the largest baseline memory footprint and the steepest upward trend, stabilizing at hundreds of MBs even for moderate request rates. This reflects JVM heap allocation strategies and background GC tuning.
\end{itemize}

From a latency perspective, Rust once again provided the most predictable response times. Go introduced occasional but tolerable pauses, while Java not only lagged in throughput but also displayed higher long-tail latencies, consistent with more expensive GC cycles.

\subsection{Cross-Workload Comparison}

Across all workloads, two patterns emerge clearly:

\begin{enumerate}
    \item \textbf{Java consistently underperformed} Rust and Go, even in the allocation-free Prime workload, highlighting that JVM overhead cannot be ignored when comparing performance-critical applications.
    \item \textbf{Rust and Go demonstrated near-parity} in compute and lightweight workloads, diverging only under heavy allocation pressure, where Rust’s manual memory management gave it more predictable performance while Go’s concurrent GC introduced minor but measurable variability.
\end{enumerate}

Taken together, these results support the conclusion that garbage collection does not always impose prohibitive overhead (as Go shows strong performance), but language runtime environments play a decisive role in determining competitiveness.

\section{Discussion}

\subsection{Research Process and Challenges}

Ensuring implementation consistency across languages presented the primary methodological challenge. Each language was implemented using its most popular web framework (Spring Boot for Java, standard library for Go, and standard library for Rust) with deliberately basic implementations to minimize framework-specific optimizations that could confound results.

A critical issue emerged during Java benchmarking when the JVM crashed due to memory exhaustion under the KV workload. This required increasing the maximum heap size to 24GB, highlighting the substantial memory overhead that managed runtimes can impose even in controlled environments. This experience reinforced the importance of proper JVM tuning in production deployments.

\subsection{Unexpected Findings and Performance Insights}

The results provide nuanced insights into the performance impact of garbage collection:

Counter to initial expectations, Java demonstrated comparable or superior throughput to both Go and Rust in certain scenarios, challenging assumptions about GC-imposed performance penalties. However, Rust consistently maintained superior latency characteristics across all workloads, suggesting that while GC languages can achieve competitive throughput, they struggle to match the predictability of manual memory management.

\begin{itemize}
    \item \textbf{JVM Runtime Overhead}: Java's significant underperformance in allocation-free workloads demonstrates that JVM-related factors (JIT warm-up, runtime checks, object model) can dominate performance, independent of garbage collection. This makes Java less competitive in microservice scenarios requiring consistent low-latency computation.
    \item \textbf{Predictability vs. Convenience}: Rust's deterministic memory management ensures stable performance across workloads. Go trades some predictability for developer productivity, but its concurrent GC mitigates the worst-case pauses effectively.
    \item \textbf{Workload Sensitivity}: Under compute-heavy workloads, GC overhead is negligible. Under allocation-heavy workloads, Rust maintains superior predictability, while Go remains competitive in throughput but exhibits occasional latency variance.
\end{itemize}

These findings suggest that the decision between GC and non-GC languages should consider not only peak throughput, but also latency guarantees and memory predictability under load.

\subsection{Limitations and Future Work}

This study's laboratory setting may not fully represent real-world deployment conditions where network latency, concurrent users, and varying hardware configurations could alter performance characteristics. The controlled environment, while necessary for isolating GC effects, abstracts away many production complexities that influence language selection decisions.

Future research should investigate performance under production-like conditions including:
\begin{itemize}
    \item Network-distributed client-server architectures with realistic network latency
    \item Varying concurrent load patterns that better simulate user behavior
    \item Different hardware configurations and resource constraints
    \item Extended runtime periods to capture long-term GC behavior and memory fragmentation effects
\end{itemize}

\subsection{Real-World Engineering Implications}

The findings provide actionable insights for software engineering teams:

\begin{itemize}
    \item \textbf{Microservices Architecture}: Rust's consistent low latency makes it well-suited for latency-critical microservices, while Go offers a balanced trade-off between performance and development velocity.
    \item \textbf{Resource-Constrained Environments}: The substantial memory overhead observed in Java (requiring 24GB heap) suggests careful consideration is needed for containerized or edge deployments.
    \item \textbf{Performance SLA Requirements}: Teams with strict tail-latency requirements should favor Rust, while those prioritizing developer productivity with acceptable latency variance may choose Go.
    \item \textbf{JVM Tuning Importance}: The Java memory crash highlights that proper JVM configuration is essential for production deployments, adding operational complexity.
\end{itemize}

\section{Conclusion}

This research evaluated the performance trade-offs between garbage-collected (Go, Java) and manually-managed (Rust) languages using real-world compute benchmarks. The experimental evidence suggests that:

\begin{itemize}
  \item Garbage collection overhead is negligible in compute-intensive scenarios with low allocation frequency.
  \item Rust offers better predictability and control, especially under allocation-heavy workloads.
  \item Go provides competitive performance with minimal developer burden, demonstrating that modern GC can achieve near-native throughput.
  \item Java, despite its mature runtime and advanced collectors, incurs additional costs unrelated to GC that limit its competitiveness against AOT-compiled languages.
\end{itemize}

Future work may extend to I/O-bound and concurrency-heavy scenarios, as well as tuning JVM parameters to investigate whether Java’s runtime overhead can be mitigated in practice.

\section{References}
\nocite{*}
\bibliographystyle{plainnat}
\bibliography{references}

\section{Appendices}
Appendices include benchmark data tables, raw CSVs, statistical analysis scripts, and extended charts such as GC pause distributions.

\end{document}
